provider_installation {
  dev_overrides {
    # Provider address must match required_providers.todoist.source
    # Update this path to wherever `go env GOPATH` points on your machine
    # (the provider binary should be in `$(go env GOPATH)/bin`).
    "andreaswwilson/todoist" = "/ABS/PATH/TO/YOUR/GOPATH/bin"
  }
  direct {}
}
